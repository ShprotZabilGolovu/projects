# Define the system of units to use internally. 
InternalUnitSystem:
  UnitMass_in_cgs:     1000    # Grams
  UnitLength_in_cgs:   100  # Centimeters
  UnitVelocity_in_cgs: 100  # Centimeters per second
  UnitCurrent_in_cgs:  1   # Amperes
  UnitTemp_in_cgs:     1   # Kelvin

Scheduler:
  max_top_level_cells: 12
  
# Parameters governing the time integration
TimeIntegration:
  time_begin: 0     # The starting time of the simulation (in internal units).
  time_end:   61536000  # The end time of the simulation (in internal units).
  dt_min:     1  # The minimal time-step size of the simulation (in internal units).
  dt_max:     864000   # The maximal time-step size of the simulation (in internal units).

# Parameters governing the conserved quantities statistics
Statistics:
  delta_time: 86400 # Time between statistics output
  
# Parameters governing the snapshots
Snapshots:
  basename:    frame   # Common part of the name of output files
  time_first:  0.           # Time of the first output (in internal units)
  delta_time:  86400          # Time difference between outputs (in internal units)

# Parameters for the self-gravity scheme
Gravity:
  eta:                      0.025     # Constant dimensionless multiplier for time integration.
  MAC:                      adaptive
  epsilon_fmm:              0.001
  theta_cr:                 0.7       # Opening angle (Multipole acceptance criterion)
  mesh_side_length:         128
  #comoving_DM_softening:         1e10 # Comoving DM softening length (in internal units).
  max_physical_DM_softening:     1e7    # Max physical DM softening length (in internal units).
  max_physical_baryon_softening:    1e7

# Parameters related to the initial conditions
InitialConditions:
  file_name:  IC.hdf5       # The file to read
  periodic:   0
  
